__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.direnv/
result
result-*
runs/
*.rrd

# STEP solids are a large exchange format and nothing loads them at runtime.
# The STL meshes ARE committed: the generated MJCF and URDF reference them, so
# the model is only self-contained with them present (156 KB total).
cad/export/*.step
